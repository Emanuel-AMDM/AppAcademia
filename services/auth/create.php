<?php

// rota executor query
// require_once('');

function sign_up($name, $email, $password, $confirm_password){

    //coloca a data e hora do dia
    $agora = date('Y-m-d H:i:s');
    $dt_created_at = $agora;
    $dt_updated_at = $agora;

    // $sql1 = "INSERT INTO tabela () VALUES ('$dt_created_at', '$dt_updated_at')";
    // executor($sql1);
}