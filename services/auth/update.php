<?php

// rota executor query
// require_once('');

function password_reset($password, $confirm_password){

    //coloca a data e hora do dia
    $agora = date('Y-m-d H:i:s');
    $dt_updated_at = $agora;

    // $sql1 = "UPDATE tabela SET coluna = 'valor', dt_updated_at = '$dt_updated_at' WHERE id = $id";
    // executor($sql1);
}